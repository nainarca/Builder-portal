# A-007 — Information Architecture Diagrams

**Companion to:** [`../A-007_Information_Architecture.md`](../A-007_Information_Architecture.md)

---

## 1. Information Domain Map

```mermaid
flowchart TB
    subgraph "Public Website"
        ID01[ID-01 Marketing & Leads]
        ID02[ID-02 Builder Onboarding]
    end
    subgraph "Super Admin Portal"
        ID03[ID-03 Organization & Tenancy]
        ID04[ID-04 White-label]
        ID05[ID-05 Subscription]
        ID06[ID-06 Platform Operations]
        ID14[ID-14 User Identity & Access]
    end
    subgraph "Builder Handover Portal"
        ID07[ID-07 Project & Unit]
        ID08[ID-08 Owner Assignment]
        ID09[ID-09 Handover Documents]
        ID10[ID-10 Invitation]
        ID12[ID-12 Reporting]
    end
    subgraph "Owner Mobile App (reference)"
        ID13[ID-13 Owner Financial & Property]
    end
    ID11[ID-11 Notification\ncross-product]
    ID15[ID-15 Future Tenant]

    ID01 --> ID02 --> ID03
    ID03 --> ID04
    ID03 --> ID05
    ID03 --> ID07
    ID07 --> ID08 --> ID10
    ID07 --> ID09
    ID10 -->|accept| ID09
    ID09 -->|transfer| ID13
    ID10 -->|accept| ID13
    ID07 --> ID12
    ID06 --> ID12
    ID14 -.resolves visibility for.-> ID03
    ID14 -.resolves visibility for.-> ID07
    ID14 -.resolves visibility for.-> ID13
```

---

## 2. Content Hierarchy Diagram

```mermaid
flowchart TB
    Org[Organization ID-03] -->|Builder-type| Project[Project ID-07]
    Project --> Unit[Unit ID-07]
    Unit --> Assignment[Owner Assignment ID-08]
    Unit --> Documents[Documents ID-09]
    Unit --> Invitation[Invitation ID-10]
    Invitation -->|accepted| Transfer[Document Transfer]
    Transfer --> OwnerOrg[Organization ID-03, Owner-type]
    OwnerOrg --> Property[Property ID-13, reference]
```

---

## 3. Information Flow Diagram

```mermaid
sequenceDiagram
    participant Visitor
    participant SuperAdmin as Super Admin (ID-02/03/04/05)
    participant Builder as Builder Portal (ID-07/08/09/10)
    participant Owner as Owner App (ID-13, reference)

    Visitor->>SuperAdmin: Builder Registration (ID-02)
    SuperAdmin->>SuperAdmin: Approve → Organization (ID-03)
    SuperAdmin->>Builder: Provisions access
    Builder->>Builder: Project/Unit (ID-07), Assignment (ID-08), Documents (ID-09)
    Builder->>Owner: Invitation (ID-10)
    Owner->>Owner: Accept → Document + Property transfer
    Owner-->>Builder: Status update (reflected in ID-12 Reporting)
```

---

## 4. Module Information Relationships

```mermaid
flowchart LR
    subgraph "Working Modules (fallback — see A-007 §2.1)"
        M1[Marketing & Conversion]
        M2[Builder Onboarding]
        M3[Tenancy & Branding]
        M4[Commercial]
        M5[Operations]
        M6[Project & Unit Prep]
        M7[Handover]
        M8[Reporting]
        M9[Communication]
        M10["Owner Property Mgmt\n(reference)"]
    end

    M1 -.-> ID01[ID-01] & ID02[ID-02]
    M2 -.-> ID02
    M3 -.-> ID03[ID-03] & ID04[ID-04]
    M4 -.-> ID05[ID-05]
    M5 -.-> ID06[ID-06]
    M6 -.-> ID07[ID-07] & ID08[ID-08]
    M7 -.-> ID09[ID-09] & ID10[ID-10]
    M8 -.-> ID12[ID-12]
    M9 -.-> ID11[ID-11]
    M10 -.-> ID13[ID-13]
```
Dashed lines indicate the relationship is provisional — traced through the fallback module reference, not a verified A-006.

---

## 5. Search Architecture Diagram

```mermaid
flowchart TB
    subgraph "Public Website — no search"
        PW[No end-user search]
    end
    subgraph "Super Admin — platform-wide, Internal-classification only"
        SA[Builders / Organizations / Users / Subscriptions]
    end
    subgraph "Builder Portal — own Organization only"
        BA[Projects / Units / Owners / Documents / Invitations]
    end
    subgraph "Owner App — own properties only (existing)"
        OA[Property / Loan / Expense / Tenant / Financial History]
    end

    SA -.never reaches.-> BA
    SA -.never reaches.-> OA
    BA -.never reaches.-> OA
    BA -.never reaches other Organizations.-> BA
```

---

## 6. Information Ownership Diagram

```mermaid
flowchart LR
    SuperAdminOwner["Super Admin owns:\nID-02, ID-06"]
    DualOwner["Super Admin + Organization co-own:\nID-03, ID-04, ID-05"]
    BuilderOwner["Builder Organization owns\n(until transfer):\nID-07, ID-08, ID-10"]
    TransferDomain["ID-09 — the one domain\nwhose owner CHANGES\n(Builder → Owner at handover)"]
    OwnerOwner["Property Owner owns,\nalways:\nID-13"]
    SharedOwner["Generated by whoever triggers it:\nID-11, ID-12"]

    BuilderOwner --> TransferDomain
    TransferDomain -->|handover acceptance| OwnerOwner
```

---

## 7. Cross Product Information Flow

```mermaid
flowchart LR
    PublicWebsite[Public Website] -->|ID-01 leads,\nID-02 registration| SuperAdmin[Super Admin]
    SuperAdmin -->|ID-03/04/05\nprovisioning| BuilderPortal[Builder Portal]
    BuilderPortal -->|ID-09/ID-10\nhandover| OwnerApp[Owner Mobile App]
    SuperAdmin -.ID-06 monitoring,\nno data transfer.-> BuilderPortal
    SuperAdmin -.ID-06 monitoring,\nno data transfer.-> OwnerApp
    BuilderPortal -.ID-12 reporting,\nderived, not transferred.-> BuilderPortal
```
